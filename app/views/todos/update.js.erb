var date = '<%= @todo.due_date.strftime("%Y%m%d")%>';
var time = '<%= @todo.due_time.strftime("%H%M")%>';
var dateTime = date + time;
var html = '<%= j render partial: 'todo', locals: {todo: @todo} %>';

console.log($(`#${dateTime}`));
$(`#${dateTime}.<%= @todo.id %>` ).replaceWith(html);